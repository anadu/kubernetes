apiVersion: v1
kind: Pod
metadata:
   name: kishore
   namespace: roboshop
spec:
  containers:
    - name: nginx
      image:  nginx
      ports:
      - containerPort: 80
    -  name: sidecar
       image: almalinux  



      